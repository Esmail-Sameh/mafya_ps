import 'package:flutter/material.dart';

class DetilsScreen extends StatelessWidget {
  const DetilsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Column(
        children: [
          Center(
            child: Text('Detills Screen'),
          ),
        ],
      ),
    );
  }
}
